<div class="col-sm-12 col-md-4 offset-md-4 mt-3">
  <div class="card">
    <div class="table-header">
      <h2 class="text-center"><%= t('edit_task') %></h2>
    </div>
    <div class="card-body">
      <%= render 'form' %>
    </div>
  </div>

  <div class="mt-3">
    <%= link_to t('back_to_goal'), goal_path(@goal), class: 'btn btn-secondary' %>
  </div>
</div>
