<div class="col-sm-12 col-md-4 offset-md-4 mt-2">
  <div class="card">
    <div class="table-header">
      <h2 class="text-center"><%= t('new_goal') %></h2>
    </div>

    <div class="card-body">
      <%= render "form" %>
    </div>
  </div>

  <div class="mt-3">
    <%= link_to t('back'), goals_path, class: 'btn btn-secondary' %>
  </div>
</div>

